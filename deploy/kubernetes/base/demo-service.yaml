apiVersion: v1
kind: Service
metadata:
  name: leaky-app
  namespace: healx
  labels:
    app: leaky-app
spec:
  type: ClusterIP
  ports:
  - port: 80
    targetPort: 8080
    protocol: TCP
    name: http
  selector:
    app: leaky-app